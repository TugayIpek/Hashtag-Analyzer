<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.analytag">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".ui.MyApplication"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.Analyze"></activity>
        <activity
            android:name=".ui.preset_hashtag.FirstCategory"
            android:label="@string/title_activity_first_category"></activity>
        <activity
            android:name=".ui.preset_hashtag.SecondCategory"
            android:label="@string/title_activity_first_category"
            tools:ignore="DuplicateActivity"></activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>